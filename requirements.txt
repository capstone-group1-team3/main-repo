# Phase 1 — data layer
pandas>=2.0
pyyaml>=6.0

# Phase 2 — graph layer
neo4j>=5.14

# config / validation (used across all phases)
pydantic>=2.6
pydantic-settings>=2.2

# Phase 3 — auth
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
pyjwt>=2.8
email-validator>=2.1
fastapi>=0.110

# Phase 4 — RAG offline
llama-index-core>=0.10
llama-index-embeddings-huggingface>=0.2
sentence-transformers>=2.7    # BGE-M3 backbone
weaviate-client>=4.5

# Phase 5-6 — agents (no extra deps beyond above)
# groq client added in Phase 9 (response agent)


# Phase 7-9 — orchestrator, action, response agents
groq>=0.9

# Phase 10 — API + monitoring
prometheus-client>=0.20
uvicorn[standard]>=0.29
httpx>=0.27
